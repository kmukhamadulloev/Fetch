# Fetch Web

Target stack: Vue 3 + TypeScript + Vite + Tailwind CSS + Pinia + Lucide Vue.

Port `prototype/fetch-ui` into reusable Vue components. Do not copy the entire prototype into one giant component.

All API calls use same-origin relative `/api/...` URLs.
